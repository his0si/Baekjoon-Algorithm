#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {

    int a = 0, b = 0;
    int sum = 0;
    
    scanf("%d %d", &a, &b);

    sum = a + b;
    printf("%d", sum);

    return 0;
}